import Dock from "./Dock";
import Container from "../Container";

export default { title: "Dock" };

export const Example = () => (
  <Container style={{ backgroundColor: "#111" }}>
    <Dock />
  </Container>
);
